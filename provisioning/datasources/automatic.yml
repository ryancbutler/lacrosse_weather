apiVersion: 1

datasources:
- name: INFLUXDB
  type: influxdb
  access: proxy
  url: http://influxdb:8086
  password: Welcome1
  user: weather
  database: weather
  basicAuth: false
  basicAuthUser:
  basicAuthPassword:
  withCredentials: false
  isDefault: true
  jsonData:
     tlsAuth: false
     tlsAuthWithCACert: false
  secureJsonData:
    password: weather
  version: 1
  editable: true